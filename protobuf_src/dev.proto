syntax = "proto3";

package debug.nested;

message Debug {
    message Nested {
        enum Number {
            A = 0;
        }

        Number number = 1;
    }

    Nested nested = 1;
    map<int32, Nested.Number> nested_map = 2;
}
